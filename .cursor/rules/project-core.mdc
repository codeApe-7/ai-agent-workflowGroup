---
description: 项目核心约定 — 全局铁律、代码注释规则与通用编码规范，所有文件始终生效
globs:
alwaysApply: true
---

# 项目核心约定

## 全局铁律

以下铁律适用于所有角色（Max、Ella、Jarvis、Kyle），不可违反：

```
1. 证据优于断言 — 任何完成声明必须附带验证证据
2. 流程不可跳过 — 工作流管道的每个环节必须走完
3. 不确定时先问 — 宁可多问一句，不要假设
```

### 证据优于断言

- 说"测试通过了" → 必须贴出测试命令和输出
- 说"功能完成了" → 必须展示验证步骤和结果
- 说"Bug 修好了" → 必须展示复现测试从失败到通过的证据
- 禁止使用："应该没问题""看起来对了""我很有信心"
- 必须使用："运行 `xxx`，结果：N/N 通过，0 失败"

### 流程不可跳过

- 非简单需求必须经过：需求澄清 → 方案设计 → 实现计划 → 开发 → 两阶段审查
- 不合并环节（不能边设计边编码）
- 不跳过审查（即使"只是小改动"）

### 不确定时先问

- 需求模糊 → 先问清楚再执行
- 技术方案不确定 → 提出 2-3 方案让用户选
- 遇到矛盾 → 指出矛盾点，等待用户澄清

## 全局 Red Flags

以下行为在任何角色中出现时，必须立即停下来纠正：

| 信号 | 正确做法 |
|------|---------|
| 需求不清就开始干活 | 停下来，先澄清需求 |
| 没有计划就写代码 | 停下来，先写实现计划 |
| 说"搞定了"但没有证据 | 运行验证命令，贴出输出 |
| 遇到 bug 直接试着修 | 停下来，走系统化调试流程 |
| 先写代码再补测试 | 删除代码，从测试开始 |
| 审查时说"看起来没问题" | 贴出具体检查证据 |
| 想跳过某个流程环节 | 不允许，除非符合简单任务豁免 |
| 一次改多个不相关的东西 | 一次只做一件事 |

## 语言与沟通

- 与用户交流使用中文
- 复杂任务先拆解再分配
- 优先提供最小可行方案，验证后再扩展

## 代码注释规则

1. 所有代码必须使用**中文注释**（函数、类、模块、关键逻辑）
2. 禁止使用尾行注释，注释必须写在代码上方单独一行
3. 一目了然的赋值语句和 import 语句不需要注释
4. 不写纯叙述性注释（如"导入模块"、"定义函数"），只注释非显而易见的意图和约束

## 开发规范

- 先读取项目已有代码，理解现有模式再动手
- 精准修改，只改需要改的部分
- 先跑通闭环再优化，不过度设计
- 考虑边界情况和错误处理
- YAGNI — 不实现当前不需要的功能
- DRY — 不重复自己
