---
description: Harness 失败案例日志 — Agent 犯错时的教训归档，供规则持续改进参考
globs:
alwaysApply: false
---

# Harness 失败案例日志

本文件记录 Agent 在执行任务过程中犯下的、现有规则本应阻止但未能阻止的错误。每条记录对应一次真实失败，以及由此触发的规则修正。

## 目的

Harness Engineering 核心原则：**Agent 的每一次失败，都是环境设计不完善的信号。** 正确的回应不是换一个更强的模型，而是重新设计它运行的环境。

## 记录格式

```
### YYYY-MM-DD — [失败简述]

**现象**：Agent 做了什么错误的事
**根因**：哪条规则缺失或不够明确
**修正**：在哪个 rule 文件中增加/修改了什么内容
**关联规则**：`rules/xxx.mdc` 第 N 行
```

## 如何使用

1. Max 或用户发现 Agent 行为偏离预期时，在此文件追加一条记录
2. 同时修改对应的 rule 文件，补充约束条目
3. 定期回顾此日志，识别高频失败模式，评估是否需要升级为 Hook 硬约束

---

## 日志条目

（以下为示例，请在实际使用中替换为真实案例）

### 2026-04-11 — 初始化日志

**现象**：首次创建 Harness 日志，暂无历史失败案例
**根因**：此前缺少失败案例归档机制
**修正**：创建本文件 + 在 `max-coordinator.mdc` 中增加反馈循环指令
**关联规则**：`rules/max-coordinator.mdc` — 持续改进章节
